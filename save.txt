{data.map((dish) => (
                <SwiperSlide key={dish.id}>
                  <DishCard
                    image={dish.image}
                    title={dish.title}
                    price={dish.price}
                    description={dish.description}
                  />
                </SwiperSlide>
              ))}